[project]
name = "cibot-test"
version = "0.1.16"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cibot",
]

[tool.uv.sources]
cibot = { git = "https://github.com/nrbnlulu/cibot.git", branch = "wip" }
